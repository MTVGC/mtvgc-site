---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contact" description="Parler de votre situation - management de transition Produit & Data pour fintech et wealthtech.">

  <section class="hero-sm">
    <div class="container">
      <p class="hero-pre">Contact</p>
      <h1>Parlons de <span class="text-gradient">votre situation</span></h1>
      <p class="hero-sub">
        Décrivez votre contexte en quelques lignes. Je reviens sous 24h.
      </p>
    </div>
  </section>

  <section class="section">
    <div class="container contact-grid">

      <form class="contact-form" name="contact" method="POST" data-netlify="true" data-netlify-honeypot="bot-field">
        <input type="hidden" name="form-name" value="contact" />
        <p class="sr-only"><label>Ne pas remplir : <input name="bot-field" /></label></p>

        <div class="form-row">
          <div class="form-group">
            <label for="firstname">Prénom</label>
            <input type="text" id="firstname" name="firstname" required placeholder="Votre prénom" />
          </div>
          <div class="form-group">
            <label for="company">Société</label>
            <input type="text" id="company" name="company" placeholder="Nom de l'entreprise" />
          </div>
        </div>

        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required placeholder="vous@entreprise.com" />
        </div>

        <div class="form-group">
          <label for="message">Votre situation</label>
          <textarea id="message" name="message" rows="6" required placeholder="Décrivez votre contexte : poste vacant, levée à préparer, contrainte réglementaire..."></textarea>
        </div>

        <button type="submit" class="btn btn-primary">Envoyer</button>
      </form>

      <aside class="contact-aside">
        <div class="card">
          <h3>Préférez un appel ?</h3>
          <p class="aside-text">
            Réservez un créneau de 30 min pour discuter de votre situation.
          </p>
          <a href="mailto:contact@mtvglobalconsulting.com?subject=Demande de rendez-vous" class="btn btn-outline aside-btn">
            Demander un créneau
          </a>
        </div>

        <div class="card aside-contact-card">
          <h3>Contact direct</h3>
          <ul class="contact-links">
            <li>
              <strong>Email</strong>
              <a href="mailto:contact@mtvglobalconsulting.com">contact@mtvglobalconsulting.com</a>
            </li>
            <li>
              <strong>LinkedIn</strong>
              <a href="https://www.linkedin.com/in/vincentvan/" target="_blank" rel="noopener">Profil LinkedIn</a>
            </li>
          </ul>
        </div>
      </aside>

    </div>
  </section>

</BaseLayout>

<style>
  .contact-grid {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr;
    gap: 3rem;
    align-items: start;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .form-group label {
    font-family: var(--font-heading);
    font-size: 0.88rem;
    color: var(--color-text-muted);
  }

  .form-group input,
  .form-group textarea {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0.75rem 1rem;
    color: var(--color-text);
    font-family: var(--font-body);
    font-size: 0.95rem;
    transition: border-color var(--transition-fast);
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px var(--color-accent-glow);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 150px;
  }

  .contact-aside {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .contact-aside h3 {
    font-size: 1rem;
    margin-bottom: 0.75rem;
    color: var(--color-primary);
  }

  .aside-text {
    color: var(--color-text-muted);
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }

  .aside-btn {
    width: 100%;
    justify-content: center;
  }

  .contact-links {
    list-style: none;
  }

  .contact-links li {
    margin-bottom: 0.75rem;
    font-size: 0.9rem;
  }

  .contact-links li strong {
    display: block;
    color: var(--color-text-bright);
    font-size: 0.82rem;
    margin-bottom: 0.1rem;
  }

  @media (max-width: 768px) {
    .contact-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>
